%tr{:id => "#{dom_id(band)}_form", :class => 'hidden'}
  = form_for band, :remote => true, :html => {:class => 'update-band-form'} do |f|
    %td= f.text_field :name
    %td
      = f.submit
      = link_to 'Cancel', '#', :class => 'cancel_update'