- title "Band"

%p
  %strong Name:
  = @band.name

%p
  = link_to "Edit", edit_band_path(@band)
  |
  = link_to "Destroy", @band, :confirm => 'Are you sure?', :method => :delete
  |
  = link_to "View All", bands_path
